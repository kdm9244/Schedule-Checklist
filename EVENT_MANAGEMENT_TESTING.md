# 일정 상세 / 수정 / 삭제

## 구현 범위

- 오늘 화면: 왼쪽 일정 목록을 항상 유지하고 가운데는 `대시보드·메모 / 일정 상세`, 오른쪽은 `일반 / 선택 일정 체크리스트`로 전환. 선택 전후 열 크기를 유지.
- 캘린더: 월간 화면의 일정을 누르면 모달이나 드로어 없이 같은 상세 작업 공간으로 전환. 돌아가면 보고 있던 월과 날짜를 유지.
- 상세 작업 공간: 일정별 체크리스트 추가·완료·삭제·순서 변경과 `완료/전체` 진행률을 즉시 반영.
- 일정 목록과 캘린더 일정에 준비 체크리스트 진행률을 표시하고, 항목이 없으면 `준비 없음`으로 표시.
- 삭제: 화면을 가리는 인라인 확인 영역 대신 일정명과 Google Calendar 반영 범위를 명시한 접근 가능한 확인 모달 사용.
- 설정: 매일·평일·주말 프리셋 또는 직접 선택한 요일에 일반 체크리스트를 하루 한 번 자동 생성. 템플릿 변경은 이후 날짜에 적용하고 기존 일일 항목은 보존.
- 로딩: 상세 작업 공간은 일정을 처음 눌렀을 때만 지연 로딩하고, 체크리스트·수정 결과는 전체 화면 재조회 없이 부분 반영.
- 설명: HTML을 실행하거나 직접 출력하지 않고 텍스트·줄바꿈·허용된 링크(http/https/mailto)로 표시.
- 수정: 제목, 날짜·시간(여러 날 포함), 종일, 장소, 설명. 설명을 수정하지 않으면 기존 HTML 서식 보존.
- 반복 일정: 선택한 인스턴스만 변경. 반복 시리즈 마스터 변경은 거부.
- 삭제: Google 삭제 후 앱 events.is_deleted=true. 체크리스트 데이터는 보존하고 일정 목록에서 숨김. 일반 체크리스트와 메모는 변경하지 않음.
- 참석자가 있는 일정의 수정/삭제는 Google sendUpdates=all로 참석자에게 통지.
- 서버에서 권한 확인 및 ETag/If-Match 충돌 방지. Google 성공 후 DB 실패 시 동일 작업 재시도로 복구.
- 저장 중 중복 제출 방지, 미저장 편집 보호, 명시적 변경 폐기, 오늘 화면 메모를 지우지 않는 부분 갱신.

## 재실행

backend 디렉터리:

```powershell
npm test
# 실제 DB 통합 테스트 포함. 임시 사용자를 만든 뒤 전체 트랜잭션을 롤백.
$env:RUN_DB_TEST='1'
npm test
```

front 디렉터리:

```powershell
node --test src/utils/*.test.js
npm run build
```

격리된 브라우저 검증 (실제 Google / DB 변경 없음):

```powershell
# backend 디렉터리, 별도 터미널
node tests/ui-server.cjs
# front 디렉터리, 별도 터미널
$env:VITE_API_ORIGIN='http://localhost:3099'
npm run dev -- --host localhost --port 5182 --strictPort
```

http://localhost:5182/calendar?date=2026-09-18 에서 테스트.
테스트 API의 데이터는 메모리에만 있으며 서버를 재시작하면 초기화된다.
일반 앱 실행 전에는 테스트 터미널을 닫거나 VITE_API_ORIGIN 환경변수를 제거한다.

## 2026-09-18 검증

- 반복 체크리스트 포함 백엔드 27개 테스트 통과(실제 PostgreSQL 롤백 통합 테스트 포함).
- 프런트 유틸 8개 테스트 통과: 날짜 경계, 종일 종료일, 타임존/초 보존, 설명 보존.
- production build 통과.
- 격리 브라우저: 인라인 상세 전환, HTML/위험 링크 제거, 일정별 체크리스트 진행률 즉시 반영, 미저장 수정 이동 차단, 명시적 변경 폐기 확인.
- 오늘 화면의 미저장 메모가 상세 화면을 열고 닫아도 유지되는 것 확인.
- 1280×720(브라우저 100%)에서 페이지와 주 작업 영역 모두 불필요한 세로·가로 스크롤이 생기지 않는 것 확인.
- 반응형 CSS 검증: 1050px 이하에서는 일정 목록과 상세를 세로 배치하고, 650px 이하에서는 상세 헤더·입력 폼을 단일 열로 전환.
- 실제 Google 계정에 대한 실등록/수정/삭제는 수행하지 않음. OAuth 로그인 후 별도 테스트 일정으로 최종 실계정 검증 필요.

## 제한

- Google API와 PostgreSQL은 하나의 원자적 트랜잭션이 아니다. 부분 실패 시 패널을 유지하고 같은 내용으로 재시도해야 한다.
- 반복 시리즈 전체 변경, 앱에서 삭제 일정 복구 UI는 이번 범위에 포함하지 않음.
- Google API가 허용하는 일부 특수 일정·초대받은 일정은 안전하게 조회 전용으로 제한.
